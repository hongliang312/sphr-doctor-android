<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <shape >
        <stroke  android:width="0.8dp" android:color="@color/wathetr" /><!-- 边框颜色，边框大小-->
        <solid   android:color="@color/wathett" /> <!-- 填充的颜色：这里设置背景透明 -->
        <corners android:radius="5dip" /><!-- android:radius 弧形的半径 -->
        <padding android:bottom="10dp"
            android:left="10dp"
            android:right="10dp"
            android:top="10dp" />
    </shape>
</selector>